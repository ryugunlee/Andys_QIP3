"""QIP4 정량 규칙이 쓰는 다년치 재무 지표 계산.

`analysis/`는 스냅샷 1행 = 1종목의 횡단면 전용이라 다년 시계열을 다룰 수 없다.
그래서 "3년 누적 영업현금흐름", "상환연수", "5년 매출 CAGR" 같은 계산은 전부
여기서 스칼라로 압축한 뒤 curated 컬럼으로 넘긴다
(`collection/financial_trend.py`의 `evaluate_uptrend`와 같은 방식).

임계값 판정은 여기서 하지 않는다 — 원시 숫자만 낸다. 임계값은 `analysis/`가
판정하므로 규칙이 바뀌어도 재수집(수 시간) 없이 재채점(수 초)만 하면 된다.
"""
